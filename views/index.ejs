<% include ./partials/header %>

<h1>Posts</h1>

<ul>
  <% posts.map((post) => { %>
      <li>
        <h3> <%= post.title %> </h3>
        <img src='<%= post.image %>'>
        <p> <%= post.body %></p>
        <p> <%= post.date %></p>
      </li>
  <% }) %>
</ul>

<% include ./partials/footer %>
